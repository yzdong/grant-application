<%=@title%>
<br>


<%= form_tag(controller: "form", action: "complete", method: "get")%>
	<%@selected_fields.each do |field|%> 
		<%= field.name%>
		<%= text_field_tag(field.name.to_sym) %>
		<br>
	<%end%>
<%= submit_tag "Apply"%>

		